# TEMPLATE ONLY. Enable only after successful backup and restore rehearsal.
[Unit]
Description=Daily MIQA Store backup

[Timer]
OnCalendar=*-*-* 03:00:00 UTC
RandomizedDelaySec=15min
Persistent=true
Unit=miqa-store-backup.service

[Install]
WantedBy=timers.target
